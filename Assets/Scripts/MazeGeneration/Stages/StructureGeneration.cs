using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StructureGeneration : GenerationStage
{
    public override MazeData ProcessMaze(MazeData mazeData)
    {
        return base.ProcessMaze(mazeData);
    }
}
